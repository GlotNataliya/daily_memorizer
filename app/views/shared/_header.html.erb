<nav class="navbar navbar-expand-lg navbar-light bg-nav">
  <div class="container-fluid">
    <div class="container">
      <form class="row d-flex">
        <div class="col-md-4">
          <div class="input-box">
            <% if @topics.present?  %>
              <div class="input-group">
                <%= search_form_for @q do |form| %>
                  <%= form.search_field :title_cont, class: "form-control border-end-0 border", placeholder: "Search" %>
                    <button class="btn btn-outline-secondary bg-white border-start-0 border-bottom-0 border ms-n5" type="button">
                      <%= form.submit "Search", class: "input-group-text btn-secondary"%>
                    </button>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
      </form>
    </div>
    <h2 class="nav-name"><%= current_user.full_name %></h2>
    <div>
      <%= link_to "Sign out", destroy_user_session_path, method: :delete, data: { "turbo-method": :delete }, class: "btn btn-nav me-4" %>
    </div>
  </div>
</nav>
